#!/bin/bash

# GitHub Initialization for Quizlet De-Blur & Paywall Remover

# 1. Initialize the git repository
git init

# 2. Add the files
git add .

# 3. Commit the initial version
git commit -m "Initial commit: Quizlet De-Blur & Paywall Remover extension"

# 4. Instructions for pushing to remote
echo "--------------------------------------------------------"
echo "Repository initialized and initial commit created."
echo "To push to a remote GitHub repository, run:"
echo "  git remote add origin <your-repo-url>"
echo "  git branch -M main"
echo "  git push -u origin main"
echo "--------------------------------------------------------"
